export const selectShows = (state) => state.shows.items;
export const selectSelectedShow = (state) => state.shows.selectedShow;
export const selectShowsLoading = (state) => state.shows.loading;
export const selectShowDetailsLoading = (state) => state.shows.detailsLoading;
export const selectShowsError = (state) => state.shows.error;
export const selectShowCity = (state) => state.shows.city;
